# Calculate the mean, median, and standard deviation of an array [1, 2, 3, 4, 5].

import numpy as np

array = np.array([1, 2, 3, 4, 5])
mean = np.mean(array)
median = np.median(array)
std_dev = np.std(array)
print("Mean:", mean)
print("Median:", median)
print("Standard Deviation:", std_dev)
